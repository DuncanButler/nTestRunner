nTestRunner

Builds and runs all the tests in a solution, displaying the results in windows Growl, Beacons 
the Visual Studio plugin or to a NUnit compatible results file.

currently code only, expects to be in the packages folder within a solution, and picks up the 
first solution file above where it is run, 

Currently only builds to dot net 4.0 framework, and machine specifications test projects.